# The CDB Client

This is the place for the CDB client and modules/packages that it may need. This
is a local client i.e. it's using the Django framework direectly, not by HTTP requests,
to perform queries.
